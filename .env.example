# ATS-ALGO — environment template. Copy to `.env` and fill in.

# Database (RDS PostgreSQL, private — reachable only from the app's security
# group, in the same AZ as the app server).
# DATABASE_URL = runtime queries.  DIRECT_URL = migrations (prisma.config.ts).
# They are the same endpoint: there is no pooler in front of the database and
# none is needed for a single long-lived server. See lib/db.ts.
#
# The ?sslmode=verify-full is REQUIRED, not hardening. RDS refuses unencrypted
# connections, and node-postgres (which @prisma/adapter-pg drives) does NOT
# negotiate TLS on its own — omit it and every runtime query dies with
# `no pg_hba.conf entry for host ..., no encryption`, surfacing through Prisma
# as the far less helpful P1010 "User was denied access on the database".
# Migrations mislead here: Prisma's migrate engine negotiates TLS by default, so
# `prisma migrate deploy` succeeds against a URL the running app cannot use.
# sslrootcert points at the RDS trust bundle so we verify we are really talking
# to AWS's database, not merely encrypting to whatever answered:
#   curl -o /etc/ats-algo/rds-ca.pem \
#     https://truststore.pki.rds.amazonaws.com/<region>/<region>-bundle.pem
DATABASE_URL="postgresql://atsadmin:<password>@<instance>.<region>.rds.amazonaws.com:5432/ats?sslmode=verify-full&sslrootcert=/etc/ats-algo/rds-ca.pem"
DIRECT_URL="postgresql://atsadmin:<password>@<instance>.<region>.rds.amazonaws.com:5432/ats?sslmode=verify-full&sslrootcert=/etc/ats-algo/rds-ca.pem"

# JWT session signing secret (generate: openssl rand -hex 48)
JWT_SECRET="replace-with-a-long-random-secret"

# Admin
ADMIN_EMAIL="admin@example.com"
ADMIN_PIN="0000"
# Superadmin: signs in through the same admin code flow, and is the only account
# allowed to delete admins and members from Admin Management.
SUPERADMIN_EMAIL="superadmin@example.com"

# SMTP (email verification)
#
# SMTP_FROM may be a bare address — lib/email.ts attaches the "ATS-ALGO" display
# name, which is the string an inbox shows in its sender column. Spell out a full
# "Name <addr>" header only to override that name.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="you@example.com"
SMTP_PASS="app-password"
SMTP_FROM="you@example.com"

# Optional: logo shown at the top of every transactional email. MUST be an
# absolute https URL to a PNG or JPG — Gmail and Outlook do not render SVG, and a
# relative path renders as a broken image. Leave it unset and the emails open with
# the ATS-ALGO wordmark set in type, which renders everywhere.
#
# This is NOT the little avatar Gmail draws next to the sender: that comes from the
# sending Google account's profile photo, or from a BIMI DNS record, and no SMTP
# message can set it. See the note at the top of lib/email.ts.
# BRAND_EMAIL_LOGO_URL="https://yourdomain.com/brand/ats-email-logo.png"

# App — the public origin, MANDATORY in production. Every verification and
# password-reset link is built from it, and it is never derived from the request
# Host header (that's attacker-controllable). The server refuses to boot if this
# is missing or localhost when NODE_ENV=production; see lib/app-url.ts.
APP_URL="https://yourdomain.com"

# Reversible encryption for stored exchange API secrets (AES-256-GCM).
# Must decode to exactly 32 bytes. Generate: openssl rand -base64 32
ENCRYPTION_KEY="replace-with-32-byte-base64-key"

# (Demo trading no longer uses env vars: connect a Bitget DEMO key in-app via
#  Account → Exchange API Connections. It's auto-detected as demo and the "Run
#  Trade" button runs on the paper engine. A live key there runs real money.)

# Stripe subscription billing
# Secret API key (test mode starts with sk_test_).
STRIPE_SECRET_KEY="sk_test_..."
# Webhook signing secret for /api/stripe/webhook (from `stripe listen` or the Dashboard).
STRIPE_WEBHOOK_SECRET="whsec_..."
# Recurring price IDs: $49/month and $559/year.
STRIPE_PRICE_MONTHLY="price_..."
STRIPE_PRICE_YEARLY="price_..."

# ── Execution engine ─────────────────────────────────────────────────────────
# The secret every TradingView alert carries in its JSON body. ONE value for all
# bots: it is entered once in the ATS indicator's settings, which has no idea
# which bot a chart is wired to. Unset means every webhook is rejected 401.
# Rotating invalidates every bot's alert at once and needs a restart.
#   openssl rand -base64 32
SIGNAL_SECRET=""
# Set to "1" to make every inbound signal a no-op without redeploying. The kill
# switch is checked before anything else in /api/signals/[botId].
SIGNALS_KILL_SWITCH=""
# Bearer token the reconcile / keep-warm crons must present.
#   openssl rand -base64 32
CRON_SECRET=""

# ── Static egress (exchange API key IP-whitelisting) ─────────────────────────
# Members can only IP-restrict their exchange keys if every call we make leaves
# from ONE fixed address. That address is the instance's Elastic IP — traffic
# goes out direct, nothing is proxied. Publish it here and the account page
# tells members to whitelist exactly this value.
#
# It MUST equal what the exchange actually sees. Verify on the box after any
# infrastructure change:  npx tsx scripts/verify-egress.ts
# Unset ⇒ the UI tells members to leave their key un-restricted.
STATIC_EGRESS_IP=""

# ── Server sizing ────────────────────────────────────────────────────────────
# Upstream Postgres connections this process opens. One long-lived server owns
# exactly one pool, so 10 is comfortable; see lib/db.ts.
DATABASE_POOL_MAX="10"
